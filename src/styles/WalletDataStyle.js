export const walletDataStyles = {
  // Outer container for the WalletData component with horizontal auto margins and padding
  container: "space-y-8 mx-auto px-6",
  // Container for displaying the loader
  loadingContainer: "flex justify-center items-center h-64",
  // Section container for each of the data sections (Assets, Transactions, Best Performing)
  sectionContainer: "p-6 bg-white dark:bg-gray-800 rounded-lg shadow-md",
  // Section title style for headings in each section (centered)
  sectionTitle: "text-xl font-semibold mb-4 text-center",
};
